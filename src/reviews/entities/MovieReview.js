export default class {
    constructor(id = undefined, movieId, author, review) {
        this.id = id;
        this.movieId = movieId;
        this.author = author;
        this.review = review;
    }
}
